
var Rooms = {
// create a storage object that can hold all of the rooms
//storage for room
  roomStorage: [],
  // once we get all of the rooms we can iterate through them and render the proper messages that are tied to the rooms
  // filter inside of rooms
  add: function(room) {
    Rooms.roomStorage.push(room);
    // go throught the room objects in the room file and filter them according to the room selected
},
};

//stores all rooms