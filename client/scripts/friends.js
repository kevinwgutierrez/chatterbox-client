var Friends = {
// create a toggleStatus
  toggleStatus: function (event) {
    //grab the attribute
    //edit the css to bold the messages
    console.log('click');
    // loop through the username property of clicked user and edit the css of each matching user
  }
};